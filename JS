Data types in JS:
  undefined, null, boolean, string, symbol, bigint, number, object
Print to console:
  console.log(objectsToBePrinted);
Escape characters:
  \\;
  \r;
  \t;
  \b;
  \n;
  \f; //form feed
Array modifiers:
  push();
  pop();
  shift();
  unshift();
